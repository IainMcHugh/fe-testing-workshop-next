const firstName = {
  label: 'First Name',
  placeholder: 'John',
  required: true,
};

const lastName = {
  label: 'Last Name',
  placeholder: 'Doe',
  required: true,
};

const phoneNumber = {
  label: 'Phone Number',
  placeholder: '0871234567',
  required: true,
};

const email = {
  label: 'Email',
  placeholder: 'john.doe@gmail.com',
  required: true,
};

const currency = {
  label: 'Currency',
  placeholder: 'EUR',
  required: true,
};

const price = {
  label: 'Price',
  placeholder: '10',
  required: true,
};

const fields = { firstName, lastName, phoneNumber, email, currency, price };

export { fields };
